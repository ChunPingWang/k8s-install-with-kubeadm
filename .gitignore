# ── Vagrant 執行期產物 ───────────────────────────────────────────────────────
.vagrant/

# ── master.sh 產生的 join 指令 ───────────────────────────────────────────────
# 內含「有效的 bootstrap token + CA 憑證公鑰雜湊」，等同叢集的入場券。
# 任何人拿到它就能把節點加進叢集，因此絕不可進版控。
join-command.sh
